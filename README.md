# Majestic RP | Family Bot

Иконки интерфейса: [Icons8](https://icons8.com) — стиль Liquid Glass.

Версия сборки: **1.914.0208**

## Возможности

- анкеты на вступление;
- личный кабинет с профилем, обращениями и AFK-системой;
- ранги и история их изменений;
- варны и история действий;
- приём и модерация откатов с капта через `/capts`;
- профиль участника;
- массовое перемещение между голосовыми каналами через `/move`.

## Хранение данных

Все рабочие данные хранятся только в PostgreSQL. Локальные JSON-файлы не используются.
Схема таблиц — в [`sql/schema.sql`](sql/schema.sql), применяется один раз через
`psql "$DATABASE_URL" -f sql/schema.sql` (или эквивалент на Railway).

Рабочие таблицы:

- `users` — текущие профили;
- `recruitment_settings` — отдельные строки Capt и RP со статусом и временем обновления;
- `tickets` — заявки с категориями `application` и `support`;
- `user_logs` — история рангов и варнов (различаются полем `log_type`);
- `afk_sessions` — активные игровые AFK-сессии с сохранённым временем завершения.
- `capt_replay_window` — состояние приёма откатов с капта (`/capts`): открыт ли приём, когда открыт, счётчик открытий и ветки на очистку.

Номера новых заявок имеют формат `<тип>-<10 цифр>`, например
`A-5831049276`.

Бот работает на Railway: сервис PostgreSQL и сервис бота живут в одном приватном
окружении (`*.railway.internal`), поэтому подключение к базе идёт без TLS.
Если переменная `POSTGRES_SSL_CA_PATH` задана, соединение вместо этого выполняется
по TLS с обязательной проверкой указанного CA-сертификата — это нужно только
для сторонних провайдеров с публичным доступом к базе, не для Railway.

Обязательные переменные `.env` (на Railway задаются как Variables сервиса):

```env
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_GUILD_ID=

POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DBNAME=

# Опционально: путь к CA-сертификату (относительно корня проекта) — включает
# обязательный TLS. Не задавайте на Railway.
POSTGRES_SSL_CA_PATH=
```

## Установка и запуск

Требуется Node.js **20.9.0 или новее**.

```bash
npm install
npm run register
npm start
```

Продакшен — Railway, подключённый к GitHub-репозиторию: пуш в `master`
запускает автосборку и передеплой сервиса `destroy-bot` автоматически, вручную
ничего перезапускать не нужно. Логи — `railway logs --service destroy-bot`.

## Обновление панелей

Бот публикует недостающие панели и обновляет существующие автоматически после запуска.

Панель личного кабинета содержит три кнопки:

- «Профиль»;
- «Обращения»;
- «AFK-система».

В канале `1291543297747194010` бот автоматически поддерживает административную
панель с разделами «Профиль», «Варны», «Ранги» и «Составы».
Все кнопки серые, а содержимое разделов показывается только нажавшему участнику
руководства.

После нажатия «Обращения» сначала показывается выпадающий список с типом
заявки (эфемерное сообщение), а после выбора типа сразу открывается форма —
набор полей зависит от выбранного типа (см. ADMIN_GUIDE.md, раздел
«Обычные обращения»). Тип сохраняется в `tickets.request_type`, все поля
формы объединяются построчно в `tickets.details`. Минимальная длина каждого
поля — 2 символа.

«Заявка на изменение ранга» видна в списке только участникам с 1 или 2
рангом, «Заявка на снятие варна» — только участникам с активным
предупреждением. Закрытие заявки на снятие варна спрашивает, выполнил ли
участник условие, и снимает один активный варн только при положительном
ответе. Закрытие заявки на изменение ранга открывает
форму с тремя вопросами (сменил фамилию / находится в планшете / посетил 25
РП мероприятий), в каждом — выпадающий список из двух вариантов ответа
(например, «Да, сменил» / «Нет, не сменил») — ранг повышается на 1 (1 → 2
или 2 → 3, с синхронизацией роли и никнейма) только если на все три ответили
утвердительно.

Раздел «Варны» административной панели показывает список условий, за
которые можно снять предупреждение — тот же список, что и в заявке на
снятие варна.

Повышение с 3 ранга и выше выдаётся только вручную, по решению руководства —
автоматических условий для этого нет. Повышение с 1 на 2 и с 2 на 3 ранг
дополнительно доступно через «Заявку на изменение ранга» (см. выше).

В `recruitment_settings` Capt и RP хранятся отдельными строками со своим статусом и временем обновления.

Перед обработкой сообщения, реакции или события участника (вход/выход/смена
ролей) бот заново загружает актуальные данные из PostgreSQL. Команды, кнопки и формы
используют кеш в памяти без блокирующего похода в базу (чтобы не рисковать
3-секундным окном ответа Discord), а сам кеш дополнительно обновляется фоном
каждые 60 секунд. Поэтому ручные изменения в панели базы данных применяются без
перезапуска бота, но не мгновенно — с задержкой до минуты.

## Команда `/move`

Доступна только ролям руководства из `leadershipRoleIds`.

- `from` — исходный голосовой канал;
- `to` — целевой голосовой канал.

Боту необходимы разрешения `Move Members`, `View Channel` и `Connect`.

## Управление заявками

Подать заявку на вступление может только участник вообще без роли в семье;
участники с 1 рангом и выше подать заявку не могут — с 1 ранга путь наверх
только через заявку на изменение ранга. Также заявку блокирует уже открытая
активная заявка этого участника или 10-дневный кулдаун после отклонения.

В заявках на вступление и обычных обращениях используются кнопки управления.
В обычном обращении доступны «Закрыть» и «Передать»: закрытие
сразу назначает нажавшего администратора ответственным и устанавливает конечный
статус «Закрыто». Передача доступна только после взятия заявки сообщением или реакцией.
В заявках на вступление остаются «Принять», «Отклонить» и «Передать».

Панель вступления и карточки заявок используют Discord Components V2. Остальные
панели и журналы используют обычные Discord-эмбеды.

Кнопка «Составы» в административной панели переключает Capt, RP или оба состава
после личного подтверждения. Состояния Capt- и RP-набора хранятся в PostgreSQL и отображаются
в панели. После отклонения заявку на вступление можно подать повторно через 10 дней.

При нажатии «Принять» бот сразу выдаёт 2 ранг — независимо от состава (RP
или CAPT) и без дополнительных вопросов. Дополнительно выдаётся одна и та же
роль Verified — `1265995505524015245`.
Отдельных ролей для RP- и Капт-состава больше нет. Просматривать и обрабатывать
заявки и обращения, а также управлять ботом через административную панель,
может любой участник с ролью из `config.json → leadershipRoleIds` (High Staff и
выше) — их же бот упоминает при создании новой заявки или обращения.

## Варны

При получении третьего активного варна бот снимает с участника все роли на
Discord-сервере (исключения из фамы не происходит). Бан, специальная роль
ограничения и система апелляций не используются.

## Управление

Роли руководства и рангов настраиваются в `config.json`. Секреты и реквизиты
PostgreSQL должны храниться только в `.env`.
